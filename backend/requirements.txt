fastapi==0.115.5
uvicorn[standard]==0.32.0
SQLAlchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
email-validator==2.1.0

# ============================================
# AI Service SDKs (optional)
# ============================================

# Core dependencies (required, supports OpenAI, DeepSeek, Ollama)
openai>=1.30.0,<3.0.0

# Optional dependencies (install as needed)
# pip install anthropic  # Anthropic Claude
# pip install google-generativeai  # Google Gemini
# pip install zhipuai  # Zhipu AI
# pip install dashscope  # Alibaba Qwen
# pip install qianfan  # Baidu Wenxin

# ============================================
# Installation commands
# ============================================
# Basic installation (mock mode):
#   pip install -r requirements.txt
#
# Install all AI services:
#   pip install -r requirements.txt
#   pip install anthropic google-generativeai zhipuai dashscope
#
# Install specific AI service:
#   pip install openai  # OpenAI + DeepSeek + Ollama
#   pip install anthropic  # Anthropic Claude
#   pip install google-generativeai  # Google Gemini
#   pip install zhipuai  # Zhipu AI
#   pip install dashscope  # Alibaba Qwen
